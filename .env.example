# ===========================================
# MCA MAKEUP - Environment Variables Template
# ===========================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env files with real credentials!

# ===========================================
# ANALYTICS CONFIGURATION
# ===========================================
# Vercel Analytics is automatically enabled when deployed to Vercel
# No additional environment variables needed for basic Analytics
# VERCEL_ANALYTICS_ID is automatically set by Vercel

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
DATABASE_URL="postgresql://username:password@localhost:5432/mca_makeup"

# ===========================================
# NEXTAUTH.JS CONFIGURATION
# ===========================================
# Generate a secure random string (32+ characters)
# You can use: openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-super-secure-random-string-here-minimum-32-chars"

# ===========================================
# EMAILJS CONFIGURATION (Respaldo para Push Notifications)
# ===========================================
# EmailJS para envío de emails desde el frontend cuando las push notifications fallen

# Obtén estas credenciales en https://www.emailjs.com/
NEXT_PUBLIC_EMAILJS_SERVICE_ID="tu_service_id_aqui"
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID="tu_template_id_aqui"
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY="tu_public_key_aqui"

# Email del administrador principal (para confirmaciones al cliente)
NEXT_PUBLIC_ADMIN_EMAIL="marcelacordero.bookings@gmail.com"

# MÚLTIPLES EMAILS PARA NOTIFICACIONES (separados por coma)
# Ejemplo: "email1@gmail.com, email2@gmail.com, email3@outlook.com"
# Si no configuras esta variable, usará NEXT_PUBLIC_ADMIN_EMAIL por defecto
NEXT_PUBLIC_ADMIN_EMAILS="marcelacordero.bookings@gmail.com, paulollanosc@gmail.com"

# ===========================================
# ADMIN CONFIGURATION
# ===========================================
# Change these before deploying to production!
ADMIN_EMAIL="admin@yourdomain.com"
ADMIN_PASSWORD="change-this-to-a-secure-password"

# ===========================================
# PUSH NOTIFICATIONS (Optional)
# ===========================================
# Para notificaciones push en tiempo real

# VAPID Keys para Web Push API
# Genera tus keys usando: npx web-push generate-vapid-keys
# O usa las keys pre-generadas del proyecto
VAPID_PUBLIC_KEY="tu_vapid_public_key_aqui"
VAPID_PRIVATE_KEY="tu_vapid_private_key_aqui"

# Clave pública para el frontend (debe coincidir con VAPID_PUBLIC_KEY)
NEXT_PUBLIC_VAPID_PUBLIC_KEY="tu_vapid_public_key_aqui"

# ===========================================
# EMAILJS CONFIGURATION (Respaldo para Push)
# ===========================================
# Para emails de respaldo cuando las push notifications fallen
# ESPECIALMENTE IMPORTANTE PARA iOS

# PASOS PARA CONFIGURAR:
# 1. Ve a https://www.emailjs.com/ y crea cuenta gratuita
# 2. Ve a "Email Services" y conecta tu email (Gmail, Outlook, etc.)
# 3. Ve a "Email Templates" y crea un template con estas variables:
#    - {{to_email}}, {{client_name}}, {{service_name}}
#    - {{appointment_date}}, {{appointment_time}}
#    - {{client_phone}}, {{client_email}}, {{total_price}}, {{notes}}
# 4. Copia las credenciales aquí:

# Credenciales de EmailJS (obtén en https://www.emailjs.com/)
NEXT_PUBLIC_EMAILJS_SERVICE_ID="service_tu_service_id_aqui"
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID="template_tu_template_id_aqui"
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY="tu_public_key_aqui"

# Email del administrador para recibir notificaciones
NEXT_PUBLIC_ADMIN_EMAIL="marcelacordero.bookings@gmail.com"

# 💡 NOTA: EmailJS funciona como respaldo automático
#    - Si las push notifications fallan, se envía email
#    - Especialmente útil en iOS donde las push tienen limitaciones
#    - No requiere configuración de servidor backend
# ===========================================
# Uncomment and set for production deployment
# NODE_ENV="production"
# NEXTAUTH_URL="https://yourdomain.com"

# ===========================================
# SECURITY NOTES
# ===========================================
# 1. Use strong, unique passwords
# 2. Generate secure random secrets
# 3. Never use default passwords in production
# 4. Consider using environment-specific secrets
# 5. Rotate secrets regularly
